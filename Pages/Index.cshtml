@page
@model IndexModel

    <div class="card_wrapper">
@for(int i = 0; i < Model.movieTitles.Count; i++) {
    <div class="card">
    <img class="card_img" src=@Model.posterURLs[i] />
    <h3 class="card_title">@Model.movieTitles[i]</h3>
    <p class="card_overview">@Model.overview[i]</p>
    </div>
}
    </div>


